/*
 * Copyright [(${#dates.format(date, 'yyyy')})] nqcx.org All right reserved. This software is the confidential and proprietary information
 * of nqcx.org ("Confidential Information"). You shall not disclose such Confidential Information and shall use
 * it only in accordance with the terms of the license agreement you entered into with nqcx.org.
 */

package [(${package})];
[# th:each="import : ${imports}"]
import [(${import})];
[/]
/**
 * @author [(${author})] [(${#dates.format(date, 'yyyy-MM-dd HH:mm')})]
 */
@Service
public class [(${name})] extends CacheSupport<[(${poName})], Long> implements [(${daoName})]<[(${poName})], Long> {

    private final ILocksJpa jpa;
    private final ILocksMapper mapper;

    public [(${name})](ILocksJpa jpa, ILocksMapper mapper) {
        this.jpa = jpa;
        this.mapper = mapper;
    }

    @Override
    protected IMapper<[(${poName})], Long> getMapper() {
        return this.mapper;
    }

    @Override
    protected IJpa<[(${poName})], Long> getJpa() {
        return this.jpa;
    }

}
